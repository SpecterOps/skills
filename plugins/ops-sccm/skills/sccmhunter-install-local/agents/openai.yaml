interface:
  display_name: Sccmhunter Install Local
  short_description: Clone SCCMHunter into operating-tools and install it in a local Python virtual environment using the
    repository's documented setup flow.
  icon_small: ./assets/icon.svg
  icon_large: ./assets/icon.png
  brand_color: '#2C2677'
  default_prompt: Use $sccmhunter-install-local for this workflow.
policy:
  allow_implicit_invocation: true
