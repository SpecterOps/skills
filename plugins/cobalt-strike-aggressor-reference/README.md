# Aggressor

Cobalt Strike Aggressor Script development skill with function references for Aggressor and Sleep.

## Contents

- `skills/cobalt-strike-aggressor-reference/` - Aggressor development workflow and references.
- `.claude-plugin/plugin.json` - Claude Code plugin manifest.
- `.codex-plugin/plugin.json` - Codex plugin manifest.

## Install

Install through the repository marketplace for full plugin packaging, or install the `cobalt-strike-aggressor-reference` skill with `npx skills` when only the skill instructions are needed.
