[project]
name = "go-review-scripts"
version = "0.3.0"
description = "Planner, artifact validation, and SARIF helpers for go-review"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.ruff]
line-length = 100
